services:
  db:
    build:
      context: .
      dockerfile: Dockerfile.postgres
    image: ${THORNHILL_POSTGRES_IMAGE:-thornhill-postgres}
    restart: unless-stopped
    environment:
      POSTGRES_USER: thornhill
      POSTGRES_PASSWORD: thornhill
      POSTGRES_DB: thornhill
    volumes:
      # PostgreSQL 18+ stores versioned clusters below /var/lib/postgresql.
      # Mount the parent so pg_upgrade --link can cross major versions safely.
      - dbdata:/var/lib/postgresql
    # The official entrypoint starts with only the capabilities needed to
    # initialize/chown a fresh volume, then drops to UID/GID 70 for postgres.
    init: true
    read_only: true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - DAC_OVERRIDE
      - FOWNER
      - SETGID
      - SETUID
    pids_limit: 256
    tmpfs:
      - /run/postgresql:rw,noexec,nosuid,size=16m,uid=70,gid=70,mode=2775
      - /tmp:rw,noexec,nosuid,size=64m,mode=1777
    security_opt:
      - no-new-privileges:true
    stop_grace_period: 30s
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U thornhill"]
      interval: 5s
      timeout: 3s
      retries: 10

  app:
    build:
      context: .
      args:
        THORNHILL_REVISION: ${THORNHILL_REVISION:-unknown}
    image: ${THORNHILL_APP_IMAGE:-thornhill-app}
    env_file: .env
    environment:
      DATABASE_URL: postgres://thornhill:thornhill@db:5432/thornhill?sslmode=disable
    ports:
      # Safe by default: local-only. Set THORNHILL_BIND_ADDR to the host's
      # Tailscale address for an intentional tailnet-only deployment.
      - "${THORNHILL_BIND_ADDR:-127.0.0.1}:8787:8787"
    volumes:
      - prebaked:/data
    init: true
    read_only: true
    cap_drop:
      - ALL
    pids_limit: 256
    tmpfs:
      - /tmp:rw,noexec,nosuid,size=64m
    security_opt:
      - no-new-privileges:true
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

volumes:
  dbdata:
  prebaked:
